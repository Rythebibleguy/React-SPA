{
  "rules": {
    "quiz_stats": {
      "$dateKey": {
        ".read": true,
        "$questionKey": {
          "$answerId": {
            ".write": true,
            ".validate": "newData.isNumber() && newData.val() >= 0"
          }
        }
      }
    }
  }
}
